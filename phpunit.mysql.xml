<?xml version="1.0" encoding="UTF-8"?>
<!--
    The same suite, against a real MySQL server.

        vendor/bin/pest -c phpunit.mysql.xml

    Needed because the default SQLite run cannot see the class of defect that
    took statamic-notifications v1.0.3 down on production: InnoDB's 3072-byte
    index limit, utf8mb4's four bytes per character, fixed column widths and
    enforced foreign keys do not exist in SQLite. Point DB_HOST/DB_PORT/
    DB_DATABASE/DB_USERNAME/DB_PASSWORD at a throwaway database; the suite
    migrates it from scratch on every run.

    This package has two reasons of its own to need it. `event_occurrences`
    carries a real foreign key to `events` with ON DELETE CASCADE — the mechanism
    that removes an event's dates with it — and SQLite does not enforce foreign
    keys unless a pragma asks it to, so a cascade that never fires looks
    identical to one that works. And `starts_at` is a TIMESTAMP holding UTC
    instants that MySQL, unlike SQLite, will happily reinterpret through the
    session timezone if the connection is configured wrongly.

    tests/Unit/IndexKeyLengthTest.php measures the key arithmetic without a
    server, so CI does not depend on this file for that. It exists for the run
    that proves the compiled DDL and the real engine agree.
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="./vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         failOnWarning="true"
         failOnRisky="true">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_TIMEZONE" value="America/Chicago"/>
        <env name="DB_CONNECTION" value="testing"/>
        <env name="DB_DRIVER" value="mysql"/>
        <env name="DB_HOST" value="127.0.0.1"/>
        <env name="DB_PORT" value="3306"/>
        <env name="DB_DATABASE" value="events_test"/>
        <env name="DB_USERNAME" value="root"/>
        <env name="DB_PASSWORD" value=""/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="BRAND_CONTEXT_MULTI_BRAND" value="false"/>
    </php>
</phpunit>
